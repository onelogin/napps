<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:layout_centerHorizontal="true"
    android:background="@drawable/splash1"
    tools:context="onelogin.com.signal.entry.EntryActivity">

    <!--background image includes app logo image in center and company logo image at bottom-->

    <!--title-->
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/title"
        android:textColor="@color/lighter"
        />

    <!--tagline-->
    <TextView
        android:id="@+id/taglineTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleTextView"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/tagline"
        android:textColor="@color/lighter"
    />

    <!--text message token agent found or not-->
    <TextView
        android:id="@+id/tokenAgentMessageTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="160dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="@string/tamessageTV"
        android:textSize="@dimen/tamessage"
        android:textColor="@color/lighter"
        />

    <!--green checkmark image
    <ImageView
        android:id="@+id/greencheckImage"
        android:layout_width="wrap_content"
        android:layout_toRightOf="@id/tamessageTV"
        />-->

    <!--continue button-->
    <Button
        android:id="@+id/actionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="74dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Continue"
        android:textSize="@dimen/button"
        android:textColor="@color/lightest"
        />

</RelativeLayout>
